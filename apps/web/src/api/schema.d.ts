/**
 * Types gerados automaticamente do OpenAPI.
 * Execute `npm run generate-types` com a API rodando para atualizar.
 *
 * Comando: npx openapi-typescript http://localhost:8000/openapi.json -o src/api/schema.d.ts
 */

// Placeholder - será substituído ao rodar generate-types
export interface paths {}
export interface components {}
export interface operations {}
